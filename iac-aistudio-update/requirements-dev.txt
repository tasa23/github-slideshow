# Minimal deps to import app.py in LOCAL_DEV mode and run the security tests.
# (The Azure/checkov/resend/OpenTelemetry packages in requirements.txt are not
# imported in LOCAL_DEV, so the test job stays fast.) Versions match
# requirements.txt where they overlap.
Flask==3.0.3
Werkzeug==3.0.4
Flask-WTF==1.3.0
Flask-Compress==1.24
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
Flask-Session==0.8.0
Flask-Limiter==3.8.0
Authlib==1.3.2
requests==2.32.3
pytest==8.3.2
