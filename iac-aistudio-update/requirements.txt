# --- Web framework ---
Flask==3.0.3
gunicorn==23.0.0
Werkzeug==3.0.4
requests==2.32.3
Flask-WTF==1.3.0
checkov==3.3.1
Flask-Compress==1.24

# --- DB & sessions ---
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
Flask-Session==0.8.0
# psycopg v3 (NOT psycopg2). Binary build for portability inside slim images.
psycopg[binary]==3.2.3

# --- Rate limiting ---
Flask-Limiter==3.8.0

# --- OAuth social login (Google + GitHub) ---
Authlib==1.3.2

# --- Azure SDKs (managed identity, Key Vault) ---
azure-identity==1.19.0
azure-keyvault-secrets==4.9.0

# --- Application Insights (auto-instruments Flask, requests, logging, SQLAlchemy) ---
# Pulls in opentelemetry-* and OTLP exporter for Azure Monitor. Pure Python deps.
azure-monitor-opentelemetry==1.6.4
resend==2.6.0
